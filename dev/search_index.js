var documenterSearchIndex = {"docs":
[{"location":"CPPLS/crossvalidation/#Cross-Validation","page":"Cross Validation","title":"Cross Validation","text":"CPPLS is a supervised method, so it is always at risk of learning structure that is only accidentally aligned with the response. Cross-validation is used to test whether the relationship learned during fitting also generalizes to samples that were not used to fit the model. In CPPLS, cross-validation also serves a second purpose: selecting the number of latent variables. These two tasks are coupled, because model complexity has a direct effect on apparent predictive performance.\n\nThe package therefore implements explicit nested cross-validation. The outer loop is used for performance assessment, whereas the inner loop is used for model selection. This keeps the choice of the number of latent variables separated from the final evaluation of the model and reduces optimistic bias.","category":"section"},{"location":"CPPLS/crossvalidation/#How-Nested-Cross-Validation-Is-Implemented-in-CPPLS","page":"Cross Validation","title":"How Nested Cross-Validation Is Implemented in CPPLS","text":"nested_cv evaluates a CPPLS workflow in the following way:\n\nThe samples are split into outer folds.\nFor each outer repeat, one fold is held out as the test set and the remaining samples  are used as the training set.\nWithin that outer training set, an inner cross-validation is run to select the number  of latent variables.\nA final model is fitted on the full outer training set with the selected number of  latent variables.\nThat model is then applied to the outer test set, and the resulting score is stored.\n\nThe implementation is explicit rather than implicit. In particular:\n\nFold construction is handled by build_folds.\nIf strata are supplied, folds are created with random_batch_indices, which shuffles  the samples within each stratum and then distributes them round-robin across folds.  This helps maintain class proportions across folds in discriminant-analysis workflows.\nThe inner-loop model selection is handled by optimize_num_latent_variables.\nFor each inner fold, CPPLS fits one model with up to max_components latent variables,  evaluates every component count from 1:max_components, and reduces those scores to a  single best component count by select_fn.\nThe final choice returned by the inner loop is the median of the per-fold best  component counts, rounded down to an integer.\n\nThis design matters because it reflects how CPPLS is actually used in the package. The outer score measures prediction on samples excluded from fitting, while the inner loop decides how complex the model is allowed to be. For classification, the helper cv_classification supplies a score function based on one-hot predictions and normalized misclassification cost. For regression, cv_regression provides the corresponding callbacks, using root mean squared error by default.\n\nBy default, the outer folds can either be reused or reshuffled between repeats. Reusing the outer folds yields a fixed repeated evaluation over a predefined partition. Enabling reshuffle_outer_folds=true instead rebuilds the outer split on each repeat, which gives broader coverage of the samples across repeated assessments.","category":"section"},{"location":"CPPLS/crossvalidation/#Why-Use-a-Permutation-Procedure?","page":"Cross Validation","title":"Why Use a Permutation Procedure?","text":"Even a carefully cross-validated supervised model can appear predictive if the response is weakly structured, the sample size is small, or the predictor matrix is high-dimensional. To address that question, CPPLS provides nested_cv_permutation, which places the entire nested cross-validation workflow inside a permutation test.\n\nThe idea is to destroy the real link between predictors and response while keeping the predictor matrix itself unchanged. CPPLS does that by permuting the rows of Y. After permutation:\n\nThe same nested CV procedure is rerun.\nThe mean outer-fold score from that permuted run is stored.\nThis is repeated many times to build a null distribution of scores expected when the  correspondence between X and Y is random.\n\nThis is useful because it tests the whole modeling pipeline, not just a single fitted model. The null distribution therefore includes the effect of repeated fold splitting, inner-loop model selection, and final outer-loop evaluation. If the score from the real, unpermuted data lies well outside what is typical under permutation, the result is more consistent with genuine predictive structure than with chance alignment. The helper calculate_p_value can then be used to summarize the observed score relative to the permutation distribution.\n\nWhen strata are supplied, the same permutation applied to Y is also applied to the stratification vector before nested CV is rerun. This keeps the stratified fold generation consistent with the permuted labels during each null-model evaluation.","category":"section"},{"location":"CPPLS/crossvalidation/#DA-Specific-Interpretation-and-Outlier-Scanning","page":"Cross Validation","title":"DA-Specific Interpretation and Outlier Scanning","text":"For discriminant analysis, ordinary cross-validation implicitly treats the provided class labels as ground truth. That means the assessment assumes that every sample assigned to a class is in fact a correct representative of that class. In controlled benchmark datasets that assumption may be reasonable, but in experimental settings it is not guaranteed. Samples can be mislabeled, contaminated, technically compromised, or simply atypical in a way that makes them behave poorly under the fitted model.\n\nFor that reason, CPPLS also provides cv_outlier_scan. This routine is not a replacement for nested CV; instead, it is a diagnostic extension for discriminant analysis. It repeatedly:\n\nbuilds an outer training/test split,\nselects the number of latent variables within the training data,\npredicts the held-out samples, and\nrecords which test samples were misclassified.\n\nAcross repeated outer folds, each sample accumulates two counts:\n\nn_tested: how many times that sample appeared in an outer test set,\nn_flagged: how many of those appearances led to a wrong class assignment.\n\nThe ratio n_flagged ./ n_tested is returned as rate. Samples with elevated rates are not automatically mislabeled, but they are candidates for closer inspection. In practice, such samples may indicate possible class-assignment problems, outlying biology, unusual measurement behavior, or incompatibility between the sample and the fitted latent structure.\n\nThis makes cv_outlier_scan especially useful in experimental DA applications, where the goal is often not only to estimate classification performance but also to identify samples that deserve follow-up quality control.","category":"section"},{"location":"CPPLS/crossvalidation/#Example","page":"Cross Validation","title":"Example","text":"We again use the synthetic discriminant-analysis dataset introduced on the Fit page. The goal here is to estimate predictive performance with nested cross-validation, compare that performance against a permutation-based null distribution, and then inspect which samples are most often misclassified across repeated outer folds.\n\nTo keep the documentation example reasonably fast, we use a fixed gamma=0.5, allow at most two latent variables, and run only a small number of folds and permutations. For a real analysis, those settings should be chosen more carefully.\n\nIn this example, class balancing is applied through obs_weight_fn, which is recomputed inside each training fold. This is preferable to precomputing inverse-frequency weights on the full dataset before cross-validation. If you instead want fixed sample-specific weights, pass them through fit_kwargs=(; obs_weights=...) and they will simply be subsetted to each training split.\n\nusing CPPLS\nusing JLD2\nusing Random\nusing Statistics\n\nsample_labels, X, classes, Y_aux = load(\n    CPPLS.dataset(\"synthetic_cppls_da_dataset.jld2\"),\n    \"sample_labels\",\n    \"X\",\n    \"classes\",\n    \"Y_aux\"\n)\n\nY, response_labels = labels_to_one_hot(classes)\ncfg = cv_classification()\n\nspec = CPPLSSpec(\n    n_components=2,\n    gamma=0.5,\n    analysis_mode=:discriminant,\n)\n\nrng = MersenneTwister(12345)\nobs_weight_fn = (X_train, Y_train; kwargs...) ->\n    invfreqweights(one_hot_to_labels(Y_train))\n\nfit_kwargs = (\n    Y_aux=Y_aux,\n    sample_labels=sample_labels,\n    response_labels=response_labels,\n)\n\nscores, best_components = nested_cv(\n    X,\n    Y;\n    spec=spec,\n    fit_kwargs=fit_kwargs,\n    obs_weight_fn=obs_weight_fn,\n    score_fn=cfg.score_fn,\n    predict_fn=cfg.predict_fn,\n    select_fn=cfg.select_fn,\n    num_outer_folds=5,\n    num_outer_folds_repeats=5,\n    num_inner_folds=4,\n    num_inner_folds_repeats=4,\n    max_components=2,\n    strata=one_hot_to_labels(Y),\n    rng=rng,\n    verbose=false,\n)\n\nobserved_accuracy = mean(scores)\n\npermutation_scores = nested_cv_permutation(\n    X,\n    Y;\n    spec=spec,\n    fit_kwargs=fit_kwargs,\n    obs_weight_fn=obs_weight_fn,\n    score_fn=cfg.score_fn,\n    predict_fn=cfg.predict_fn,\n    select_fn=cfg.select_fn,\n    num_permutations=999,\n    num_outer_folds=5,\n    num_outer_folds_repeats=5,\n    num_inner_folds=4,\n    num_inner_folds_repeats=4,\n    max_components=2,\n    strata=one_hot_to_labels(Y),\n    rng=MersenneTwister(12345),\n    verbose=false,\n)\n\np_value = calculate_p_value(permutation_scores, observed_accuracy; tail=:upper)\n\n(\n    observed_accuracy=observed_accuracy,\n    best_components=best_components,\n    permutation_mean=mean(permutation_scores),\n    p_value=p_value,\n)\n\nThe returned best_components vector contains the component count selected inside each outer repeat. The observed_accuracy is the mean nested-CV score on the real labels, whereas permutation_scores summarize the same workflow after the correspondence between predictors and class labels has been broken. The empirical p-value therefore answers a pipeline-level question: is the observed accuracy unusual compared with what the same analysis achieves under random label assignments? Because classification uses an accuracy-like score here, calculate_p_value is called with tail=:upper so that large observed scores are treated as stronger evidence against the null model.\n\nWhen the focus shifts from global performance to potentially problematic samples, cv_outlier_scan can be used as a follow-up diagnostic.\n\noutlier_scan = cv_outlier_scan(\n    X,\n    classes;\n    spec=spec,\n    fit_kwargs=(; Y_aux=Y_aux, sample_labels=sample_labels),\n    obs_weight_fn=obs_weight_fn,\n    num_outer_folds=3,\n    num_outer_folds_repeats=6,\n    num_inner_folds=3,\n    num_inner_folds_repeats=3,\n    max_components=2,\n    rng=MersenneTwister(54321),\n    verbose=false,\n)\n\nsuspect_idx = sortperm(outlier_scan.rate, rev=true)[1:5]\n\n(\n    sample=sample_labels[suspect_idx],\n    class=classes[suspect_idx],\n    tested=outlier_scan.n_tested[suspect_idx],\n    flagged=outlier_scan.n_flagged[suspect_idx],\n    rate=round.(outlier_scan.rate[suspect_idx]; digits=3),\n)\n\nThese rates are not formal proof that a sample is mislabeled. They are a practical way to prioritize inspection of samples that repeatedly fail when they are held out, which is often exactly the situation in which class-assignment problems or unusual sample behavior become visible.","category":"section"},{"location":"CPPLS/crossvalidation/#CPPLS.calculate_p_value","page":"Cross Validation","title":"CPPLS.calculate_p_value","text":"calculate_p_value(\n    permutation_scores::AbstractVector{<:Real},\n    observed_score::Float64;\n    tail::Symbol=:upper,\n)\n\nCompute an empirical p-value from permutation scores. With tail=:upper, the p-value is the fraction of permutation scores greater than or numerically equal to the observed score, with a +1 correction in both numerator and denominator to account for the observed model itself. This is appropriate for accuracy-like metrics where larger values are better. With tail=:lower, the comparison is reversed, which is appropriate for error-like metrics where smaller values are better.\n\nArguments\n\npermutation_scores: vector of scores from label-shuffled runs.\nobserved_score: score achieved by the model fit to the original data.\ntail: comparison direction, either :upper or :lower.\n\nExample\n\njulia> calculate_p_value([0.4, 0.5, 0.55, 0.6], 0.58) ≈ 0.6\ntrue\n\njulia> calculate_p_value([0.4, 0.5, 0.55, 0.6], 0.58; tail=:lower) ≈ 0.8\ntrue\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/crossvalidation/#CPPLS.cv_classification","page":"Cross Validation","title":"CPPLS.cv_classification","text":"cv_classification(; weighted::Bool=true)\n\nReturn a named tuple containing score_fn, predict_fn, select_fn, and flag_fn suited to CPPLS classification with one-hot response matrices. The scoring rule is based on nearest-mean classification and assumes models were fit in :discriminant mode.\n\nThis helper is meant to supply the callback interface expected by nested_cv, nested_cv_permutation, and cv_outlier_scan. In particular, score_fn, predict_fn, and select_fn can be passed directly to nested_cv and nested_cv_permutation, while flag_fn is used by cv_outlier_scan to count misclassified samples across repeated outer folds.\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/crossvalidation/#CPPLS.cv_regression","page":"Cross Validation","title":"CPPLS.cv_regression","text":"cv_regression(;\n    score_fn=(Y_true, Y_pred) -> sqrt(mean((Y_true .- Y_pred) .^ 2)),\n    select_fn=argmin,\n)\n\nReturn a named tuple containing score_fn, predict_fn, and select_fn suited to CPPLS regression. By default, predictions are scored with root mean squared error and the selected number of components minimizes that loss.\n\nThis helper is meant to supply the callback interface expected by nested_cv and nested_cv_permutation for regression problems. The returned predict_fn extracts the prediction matrix corresponding to the requested number of components.\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/crossvalidation/#CPPLS.cv_outlier_scan","page":"Cross Validation","title":"CPPLS.cv_outlier_scan","text":"cv_outlier_scan(X::AbstractMatrix{<:Real}, Y::AbstractMatrix{<:Real};\n    spec::CPPLSSpec,\n    fit_kwargs::NamedTuple=(;),\n    obs_weight_fn::Union{Function, Nothing}=nothing,\n    num_outer_folds::Integer=8,\n    num_outer_folds_repeats::Integer=10 * num_outer_folds,\n    num_inner_folds::Integer=7,\n    num_inner_folds_repeats::Integer=num_inner_folds,\n    max_components::Integer=spec.n_components,\n    reshuffle_outer_folds::Bool=true,\n    rng::AbstractRNG=Random.GLOBAL_RNG,\n    verbose::Bool=true)\n\nRun repeated nested cross-validation for classification and count how often each sample is tested and misclassified. The result is a named tuple containing n_tested, n_flagged, and rate = n_flagged ./ n_tested. By default, reshuffle_outer_folds = true so samples are re-evaluated across different outer splits. This routine uses the callback bundle returned by cv_classification().\n\n\n\n\n\ncv_outlier_scan(\n    X::AbstractMatrix{<:Real},\n    sample_classes::AbstractCategoricalArray; \n    kwargs...\n)\n\nConvert categorical sample classes to one-hot form and forward to cv_outlier_scan.\n\n\n\n\n\ncv_outlier_scan(\n    X::AbstractMatrix{<:Real}, \n    sample_classes::AbstractVector; \n    kwargs...\n)\n\nConvert non-numeric sample classes to one-hot form and forward to cv_outlier_scan. Numeric vectors are rejected because they are ambiguous with regression targets.\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/crossvalidation/#CPPLS.nested_cv","page":"Cross Validation","title":"CPPLS.nested_cv","text":"nested_cv(X::AbstractMatrix{<:Real}, Y::AbstractMatrix{<:Real};\n    spec::CPPLSSpec,\n    fit_kwargs::NamedTuple=(;),\n    obs_weight_fn::Union{Function, Nothing}=nothing,\n    score_fn::Function,\n    predict_fn::Function,\n    select_fn::Function,\n    num_outer_folds::Int=8,\n    num_outer_folds_repeats::Int=num_outer_folds,\n    num_inner_folds::Int=7,\n    num_inner_folds_repeats::Int=num_inner_folds,\n    max_components::Int=spec.n_components,\n    strata::Union{AbstractVector{<:Int}, Nothing}=nothing,\n    reshuffle_outer_folds::Bool=false,\n    rng::AbstractRNG=Random.GLOBAL_RNG,\n    verbose::Bool=true)\n\nRun explicit nested cross-validation for CPPLS. The caller supplies score_fn, predict_fn, and select_fn, so the routine can be used for either regression or classification. spec and fit_kwargs control model fitting, while the return value is (outer_fold_scores, optimal_num_latent_variables).\n\nWhen provided, obs_weight_fn is called on each training split as obs_weight_fn(X_train, Y_train; sample_indices=..., fit_kwargs=..., spec=...). The callback must return fold-local observation weights matching the current training-set size, or nothing. Any fold-local weights returned by the callback are combined elementwise with fixed obs_weights supplied through fit_kwargs.\n\nFor standard use, these callbacks can be obtained from cv_classification() or cv_regression().\n\nWhen spec.analysis_mode == :discriminant, default response_labels are injected if they are not already present in fit_kwargs.\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/crossvalidation/#CPPLS.nested_cv_permutation","page":"Cross Validation","title":"CPPLS.nested_cv_permutation","text":"nested_cv_permutation(X::AbstractMatrix{<:Real}, Y::AbstractMatrix{<:Real};\n    spec::CPPLSSpec,\n    fit_kwargs::NamedTuple=(;),\n    obs_weight_fn::Union{Function, Nothing}=nothing,\n    score_fn::Function,\n    predict_fn::Function,\n    select_fn::Function,\n    num_permutations::Int=999,\n    num_outer_folds::Int=8,\n    num_outer_folds_repeats::Int=num_outer_folds,\n    num_inner_folds::Int=7,\n    num_inner_folds_repeats::Int=num_inner_folds,\n    max_components::Int=spec.n_components,\n    strata::Union{AbstractVector{<:Int}, Nothing}=nothing,\n    reshuffle_outer_folds::Bool=false,\n    rng::AbstractRNG=Random.GLOBAL_RNG,\n    verbose::Bool=true)\n\nRun a permutation test around nested_cv by repeatedly permuting the rows of Y and recomputing the nested cross-validation score. The result is a vector of mean scores, one for each permutation. As in nested_cv, the required callbacks can be obtained from cv_classification() or cv_regression().\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/theory/#Theory","page":"Theory","title":"Theory","text":"Canonical Powered Partial Least Squares (CPPLS) is a supervised projection method for regression and classification. Its purpose is to extract latent components that summarize the predictor matrix X in mathbbR^n times p, where n is the number of samples and p the number of predictors, in a way that best reflects the structure in a multivariate response matrix Y in mathbbR^n times q, where q is the number of response variables. The method extends standard PLS in three important ways. First, it allows multiple response variables, including both primary responses that one wishes to predict and optional auxiliary responses that guide the extraction of components. Second, it incorporates a power parameter gamma that controls the balance between predictor variance and predictor–response correlation, giving the user explicit control over how strongly the model should emphasize correlation structure. Third, CPPLS can operate with a vector of non-negative sample weights, allowing some observations to contribute more or less to the fitted model. This is useful when classes are unbalanced, when some samples are more reliable, or when experimental design considerations suggest that certain samples should carry greater influence.\n\nEach CPPLS component is extracted in two conceptual stages. First, the predictors are projected onto supervised directions, one for each column of Y, using the gamma-controlled mixture of weighted predictor variance and weighted predictor–response correlation. Second, a canonical correlation analysis (CCA) determines how these supervised directions should be linearly combined into a single latent variable that is optimally aligned with the primary responses. Auxiliary responses and observation weights enter the computation of supervised directions in the first stage, shaping the latent space that is subsequently analyzed by CCA, while the CCA itself is guided solely by the primary responses under the same weighting structure.","category":"section"},{"location":"CPPLS/theory/#Weighted-Preprocessing","page":"Theory","title":"Weighted Preprocessing","text":"To begin, X and Y are centered using the supplied sample weights. If w_i is the weight of sample i and the weights are normalized to sum to one, the weighted mean of a variable x becomes\n\nbarx = sum_i w_i x_i \n\nAll variances, covariances, and correlations are computed in a weighted sense. For a centered variable x, the weighted variance is\n\noperatornameVar_w(x) = sum_i w_i x_i^2 \n\nand for two centered variables x and y, the weighted covariance is\n\noperatornameCov_w(xy) = sum_i w_i x_i y_i \n\nWeighted correlations are obtained by normalizing the weighted covariance by the corresponding weighted standard deviations. For two centered variables x and y, the weighted correlation is\n\noperatornamecorr_w(xy) =\nfracoperatornameCov_w(xy)\nsqrtoperatornameVar_w(x)operatornameVar_w(y) ","category":"section"},{"location":"CPPLS/theory/#Supervised-Compression","page":"Theory","title":"Supervised Compression","text":"CPPLS constructs a supervised transformation matrix by combining predictor scale and predictor–response correlation, with the balance controlled by the power parameter gamma in 01. The expressions below are written for the interior case 0  gamma  1; the endpoint values gamma = 0 and gamma = 1 are handled in CPPLS as limiting cases. For each predictor x_j (a column of X) and each response y_k (a column of Y), CPPLS computes the weighted standard deviation operatornamestd_w(x_j) and the weighted correlation operatornamecorr_w(x_jy_k). These quantities are not combined additively, but multiplicatively through gamma-dependent powers.\n\nThe resulting supervised weight matrix is\n\nW_0(gamma) in mathbbR^p times q\n\nwhere p is the number of predictors and q is the number of response columns, including auxiliary ones. It can be written as a product of a diagonal scale matrix and a correlation matrix,\n\nW_0(gamma) = S_x(gamma)C(gamma)\n\nwith diagonal entries\n\nS_x(gamma)_jj = operatornamestd_w(x_j)^frac1-gammagamma\n\nand correlation entries\n\nC(gamma)_jk = operatornamesignbig(operatornamecorr_w(x_jy_k)big) leftoperatornamecorr_w(x_jy_k)right^fracgamma1-gamma \n\nThus, each entry of W_0(gamma) is proportional to a product of a predictor standard-deviation term, which serves as a measure of predictor scale, and a predictor–response correlation term, each raised to a power determined by gamma. When gamma is small, predictors with large weighted standard deviation are emphasized; when gamma approaches one, predictors that are strongly correlated with the responses dominate. In traditional PLS workflows, predictor scaling is often used to control whether high-variance variables dominate the model. In CPPLS, the relative influence of predictor scale and predictor–response correlation is instead controlled explicitly through the power parameter gamma within the weight construction, and the model evaluates candidate values of gamma to identify the supervised representation of X that is best aligned with Y. This reduces the need for separate scaling decisions as a preprocessing step while making the variance-correlation trade-off part of the fitted model itself.\n\nEach column of W_0(gamma) admits a direct geometric interpretation. For each response variable y_k, CPPLS constructs a direction in the original predictor space that emphasizes predictors with large weighted variance and predictors that are strongly correlated with that response, with the balance controlled by the power parameter gamma. These directions represent response-specific supervised views of the predictor space.\n\nProjecting the predictor matrix onto these directions,\n\nZ(gamma) = X W_0(gamma) \n\nmaps each sample from the original p-dimensional predictor space into a q-dimensional supervised space, where each coordinate summarizes the sample along a direction tailored to one column of Y. Auxiliary response variables contribute additional supervised directions and thereby enrich this intermediate representation. In this sense, W_0(gamma) defines a supervised low-dimensional coordinate system for the predictors. Every entry in Z(gamma) = X W_0(gamma) is a single scalar summary value for a sample in the supervised direction associated with a response column. This representation is an intermediate supervised projection of X and does not yet define a CPPLS component; rather, it provides the response-guided axes that are subsequently combined by canonical correlation analysis to form the final latent component.","category":"section"},{"location":"CPPLS/theory/#Choosing-the-Power-Parameter","page":"Theory","title":"Choosing the Power Parameter","text":"The power parameter gamma can either be supplied directly by the user or selected by CPPLS from a user-defined interval or grid of candidate values. When gamma is selected rather than fixed, CPPLS evaluates each candidate value by constructing the supervised compression\n\nZ(gamma) = X W_0(gamma) \n\nwhere the matrix W_0(gamma) depends on gamma through a power-based trade-off between predictor scale, represented by weighted standard deviations, and predictor–response association, represented by weighted correlations. CPPLS then performs a weighted canonical correlation analysis between Z(gamma) and the primary response block Y_mathrmprim, and records the first (largest) canonical correlation\n\nrho_1(gamma) = operatornameccorr_wbig(Z(gamma) Y_mathrmprimbig) \n\nas a score for that value of gamma. The optimal value\n\ngamma_mathrmbest = argmax_gamma in mathcalG rho_1(gamma)\n\nis therefore the gamma whose variance–correlation–weighted construction of Z(gamma) yields a representation of X that is maximally aligned, in the canonical correlation sense, with the primary responses. This step does not yet extract latent components or deflate the data; it only compares candidate supervised representations under identical conditions in order to select gamma. If the user provides a fixed gamma, this selection step is skipped and CPPLS proceeds directly with that value.","category":"section"},{"location":"CPPLS/theory/#Component-Extraction","page":"Theory","title":"Component Extraction","text":"Once the optimal gamma has been determined, CPPLS recomputes\n\nZ = Z(gamma_mathrmbest)\n\nand performs a full weighted CCA between this matrix and the primary response columns of Y. The result is a canonical direction a in the Z-space and a corresponding direction b in the primary response space. The direction a specifies how to combine the supervised directions in Z into one axis that maximally correlates with the primary responses.  Providing Y_mathrmaux changes the supervised directions W_0, so the intermediate representation Z = X W_0 reflects both the primary responses and auxiliary structure. The CCA direction a is still chosen only to align Z with Y_mathrmprim, but it is chosen inside a supervised space that already accounts for systematic variation captured by Y_mathrmaux. In practice, this means auxiliary variables can steer the construction of the latent space without becoming prediction targets themselves.\n\nThe canonical direction is then mapped back into the predictor space through\n\nw = W_0(gamma_mathrmbest) a \n\nproducing the final CPPLS weight vector. This vector lies in the original predictor space and defines the direction used to compute the component score\n\nt = X w \n\nThe component score t acts as a latent one-dimensional slider: each sample receives a coordinate t_i, and moving along this latent axis corresponds to sliding along the component in predictor space. The relationship between the component and the original variables is captured by the loadings. The weighted X-loading is given by\n\np = fracX^top W tt^top W t\n\nwhich is the weighted regression of the predictors on the component. Each entry of p is therefore a linear coefficient describing how the corresponding predictor changes, on average, as one moves along t. The weighted Y-loading\n\nc = fracY^top W tt^top W t\n\nlikewise contains linear coefficients describing how each response variable, including auxiliary responses when present, varies with the component under the weighting structure.\n\nDeflation removes the part of X and Y that can be explained by this component:\n\nX leftarrow X - t p^topqquad\nY leftarrow Y - t c^top \n\nAfter this deflation, the dominant structure captured by the current component has been removed from both X and Y. Because subsequent components are extracted from the deflated matrices, they often describe remaining structured variation rather than repeating the same dominant signal. In discriminant analysis, for example, the first component may capture most of the class separation, while later components may describe residual within-class structure or additional sources of systematic variation.","category":"section"},{"location":"CPPLS/theory/#Sample-Weights-and-Auxiliary-Responses","page":"Theory","title":"Sample Weights and Auxiliary Responses","text":"Sample weighting becomes particularly important in discriminant analysis (CPPLS-DA) when classes are unbalanced, when some samples are more reliable than others, or when the sampling design itself introduces unequal representation. For example, if one class has 20 samples and another 60, an unweighted analysis gives the larger class three times the influence on weighted variances, covariances, and correlations. The supervised compression can then be driven more by variation inside the majority class than by the separation between classes. By assigning larger weights to the minority class and smaller weights to the majority class, the effective contribution of each class can be balanced. The resulting components better reflect between-class structure instead of mostly tracking the most common samples.\n\nWeights can also compensate for auxiliary structure that is unevenly distributed across classes. If collection time, instrument batch, or another nuisance factor is associated more strongly with one class than another, that imbalance can distort the estimated correlations used to build W_0(gamma). Reweighting samples can reduce this distortion by making the weighted covariance structure better reflect the comparison of interest. Similarly, samples known to be noisy or unreliable can be down-weighted, while representative or carefully controlled samples can be up-weighted, improving the robustness of the extracted components.\n\nAuxiliary responses address the problem of structured variation in X that is not itself the primary target but can still influence the extracted components. Instead of changing how much influence each sample has, they tell CPPLS which additional structured variation should be represented when constructing the supervised space. If a nuisance factor such as collection date, instrument batch, or processing condition explains part of the variation in X, adding it as a column in Y_mathrmaux gives CPPLS a dedicated supervised direction for that factor. The primary responses are still the only targets used when selecting the final canonical direction and when building the prediction model, but that direction is now chosen in a latent space that has already organized part of the nuisance variation explicitly. This reduces the risk that nuisance structure is absorbed into the primary component simply because it happens to correlate with the target labels in the observed sample.\n\nA concrete example illustrates the benefit of combining auxiliary responses and sample weighting. Suppose two insect species are analyzed by GC-MS to characterize their cuticular hydrocarbons. The primary task is to discriminate species, but chemical profiles also change with season, and the two species may not be collected uniformly throughout the year. In this situation, the largest variation in X may reflect seasonal drift rather than species. If season is omitted from the response structure, peaks that vary with collection date may appear spuriously associated with species because the sampling times differ. Including collection date or season as an auxiliary response gives CPPLS a supervised direction that represents this temporal effect explicitly. If the sampling is also imbalanced, class-balanced weights can prevent one species or one part of the season from dominating the covariance estimates used to build the components. Together, auxiliary responses and sample weights help separate the biological signal of interest from structured sampling effects, producing a more stable and interpretable model.","category":"section"},{"location":"CPPLS/theory/#Final-Regression-Model","page":"Theory","title":"Final Regression Model","text":"After all components are extracted, regression coefficients for predicting the primary responses are assembled using\n\nB =\nW_mathrmcomp\nleft( P^top W_mathrmcomp right)^-1\nC_mathrmprimary^top \n\nwhere W_mathrmcomp contains the component weight vectors, P the corresponding X-loadings, and C_mathrmprimary the primary Y-loadings.\n\nOnly the primary response block contributes to C_mathrmprimary and thus to the final coefficient matrix B. Auxiliary responses affect the fitted model indirectly, by shaping the supervised compression and therefore the extracted components, but they are not themselves predicted.","category":"section"},{"location":"CPPLS/theory/#Summary","page":"Theory","title":"Summary","text":"In summary, CPPLS combines three complementary forms of supervision: the power parameter gamma that controls the balance between variance and correlation, auxiliary responses that provide additional structured guidance for the supervised compression, and sample weights that ensure appropriate influence of different samples or classes. Together, these features allow CPPLS to build stable, interpretable, and discriminative models even in complex, high-dimensional, and confounded data settings.","category":"section"},{"location":"Utils/encoding/#Encoding","page":"Encoding","title":"Encoding","text":"","category":"section"},{"location":"Utils/encoding/#CPPLS.labels_to_one_hot","page":"Encoding","title":"CPPLS.labels_to_one_hot","text":"labels_to_one_hot(label_indices::AbstractVector{<:Integer}, n_labels::Integer)\n\nConvert 1-based integer label indices to a dense one-hot encoded matrix with n_labels columns. This method assumes the set of classes is already known and returns only the encoded matrix.\n\nExamples\n\njulia> labels_to_one_hot([1, 3, 2], 3)\n3×3 Matrix{Int64}:\n 1  0  0\n 0  0  1\n 0  1  0\n\n\n\n\n\nlabels_to_one_hot(labels::AbstractVector)\n\nEncode arbitrary labels such as strings, integers, or symbols into a one-hot matrix, automatically determining the label ordering. The result is a tuple containing the encoded matrix and the ordered labels, so predictions can be mapped back to the original domain.\n\nExamples\n\njulia> matrix, classes = labels_to_one_hot([\"cat\", \"dog\", \"cat\"])\n([1 0; 0 1; 1 0], [\"cat\", \"dog\"])\n\n\n\n\n\n","category":"function"},{"location":"Utils/encoding/#CPPLS.one_hot_to_labels","page":"Encoding","title":"CPPLS.one_hot_to_labels","text":"one_hot_to_labels(one_hot_matrix::AbstractMatrix{<:Integer})\n\nDecode one-hot rows back into label indices by selecting the column of the maximum entry in each row. This works with integer-valued matrices whose rows encode a single selected label.\n\nExamples\n\njulia> one_hot_to_labels([1 0 0; 0 1 0; 0 0 1])\n3-element Vector{Int64}:\n 1\n 2\n 3\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/types/#Types","page":"Types","title":"Types","text":"CPPLS relies on two main custom types in the StatsAPI.fit and StatsAPI.predict  workflow. CPPLSSpec stores the user-controlled model specification, including  the number of components, the gamma configuration, centering, the analysis mode, and  numerical tolerances. CPPLSFit is the full fitted model returned by fit;  it contains regression coefficients, scores, labels, and other quantities needed for  prediction, diagnostics, and plotting.\n\nA third container, CPPLSFitLight, stores only the subset of fitted-model information needed for cross-validation. It is mainly an internal optimization and is typically not accessed directly by users.\n\nThe package also provides getters for fields that are commonly useful in downstream work. For CPPLSSpec, the getters are analysis_mode, gamma,  and n_components. For CPPLSFit, the getters are coef,  analysis_mode, fitted, gamma, predictor_labels,  response_labels, residuals, sample_classes,  sample_labels, and X_scores.\n\nBoth container types expose additional named fields. You can inspect them with names(spec) or names(model) and access them via dot notation, for example spec.X_loading_weight_tolerance.","category":"section"},{"location":"CPPLS/types/#CPPLSSpec","page":"Types","title":"CPPLSSpec","text":"","category":"section"},{"location":"CPPLS/types/#AbstractCPPLSFit","page":"Types","title":"AbstractCPPLSFit","text":"","category":"section"},{"location":"CPPLS/types/#CPPLSFit","page":"Types","title":"CPPLSFit","text":"","category":"section"},{"location":"CPPLS/types/#CPPLSFitLight","page":"Types","title":"CPPLSFitLight","text":"","category":"section"},{"location":"CPPLS/types/#CPPLS.CPPLSSpec","page":"Types","title":"CPPLS.CPPLSSpec","text":"CPPLSSpec{T}\n\nModel specification passed to fit. A CPPLSSpec stores the user-controlled settings for CPPLS fitting, most importantly n_components, gamma, center, and analysis_mode.\n\n\n\n\n\n","category":"type"},{"location":"CPPLS/types/#CPPLS.analysis_mode-Tuple{CPPLSSpec}","page":"Types","title":"CPPLS.analysis_mode","text":"analysis_mode(spec::CPPLSSpec)\n\nReturn the analysis mode requested in the specification.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.gamma-Tuple{CPPLSSpec}","page":"Types","title":"CPPLS.gamma","text":"gamma(spec::CPPLSSpec)\n\nReturn the gamma requested in the specification.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.n_components-Tuple{CPPLSSpec}","page":"Types","title":"CPPLS.n_components","text":"n_components(spec::CPPLSSpec)\n\nReturn the number of components requested in the specification.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.AbstractCPPLSFit","page":"Types","title":"CPPLS.AbstractCPPLSFit","text":"AbstractCPPLSFit\n\nCommon supertype for fitted CPPLS models that share the fields B, Xbar, Ybar, and analysis_mode.\n\n\n\n\n\n","category":"type"},{"location":"CPPLS/types/#StatsAPI.coef-Tuple{AbstractCPPLSFit}","page":"Types","title":"StatsAPI.coef","text":"coef(model::AbstractCPPLSFit)\ncoef(model::AbstractCPPLSFit, n_components::Integer)\n\nReturn the regression coefficient matrix for the final (or requested) number of components.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.CPPLSFit","page":"Types","title":"CPPLS.CPPLSFit","text":"CPPLSFit{T1, T2}\n\nFull fitted CPPLS model returned by fit. This type stores regression coefficients together with the intermediate quantities needed for diagnostics, projections, and plotting.\n\nMost users will work with a CPPLSFit through predict, project, coef, fitted, residuals, X_scores, and the various label getters rather than by accessing fields directly.\n\n\n\n\n\n","category":"type"},{"location":"CPPLS/types/#CPPLS.analysis_mode-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.analysis_mode","text":"analysis_mode(cpplsfit::CPPLSFit)\n\nReturn the analysis mode for the fitted model.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#StatsAPI.fitted-Tuple{CPPLSFit}","page":"Types","title":"StatsAPI.fitted","text":"fitted(model::CPPLSFit)\nfitted(model::CPPLSFit, n_components::Integer)\n\nReturn the fitted response matrix for the final (or requested) number of components.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.gamma-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.gamma","text":"gamma(cpplsfit::CPPLSFit)\n\nReturn the power-parameter values selected during fitting.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.predictor_labels-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.predictor_labels","text":"predictor_labels(cpplsfit::CPPLSFit)\n\nReturn the stored predictor labels for the fitted model.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.response_labels-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.response_labels","text":"response_labels(cpplsfit::CPPLSFit)\n\nReturn the response labels (response names or class names) for the fitted model.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#StatsAPI.residuals-Tuple{CPPLSFit}","page":"Types","title":"StatsAPI.residuals","text":"residuals(model::CPPLSFit)\nresiduals(model::CPPLSFit, n_components::Integer)\n\nReturn the response residual matrix for the final (or requested) number of components.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.sample_classes-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.sample_classes","text":"sample_classes(cpplsfit::CPPLSFit)\n\nReturn the per-sample class labels stored for discriminant analysis models, or nothing for regression fits.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.sample_labels-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.sample_labels","text":"sample_labels(cpplsfit::CPPLSFit)\n\nReturn the stored sample labels for the fitted model.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.X_scores-Tuple{CPPLSFit}","page":"Types","title":"CPPLS.X_scores","text":"X_scores(cpplsfit::CPPLSFit)\n\nReturn the predictor score matrix T for the fitted model.\n\n\n\n\n\n","category":"method"},{"location":"CPPLS/types/#CPPLS.CPPLSFitLight","page":"Types","title":"CPPLS.CPPLSFitLight","text":"CPPLSFitLight{T}\n\nReduced fitted CPPLS model that retains only the information needed for prediction. This type is used mainly for efficient internal prediction during cross-validation.\n\nMost users will work with CPPLSFit instead.\n\n\n\n\n\n","category":"type"},{"location":"CPPLS/visualization/#Visualization","page":"Visualization","title":"Visualization","text":"","category":"section"},{"location":"CPPLS/visualization/#CPPLS.scoreplot","page":"Visualization","title":"CPPLS.scoreplot","text":"scoreplot(samples, groups, scores; backend=:plotly, kwargs...)\nscoreplot(cppls; backend=:plotly, kwargs...)\n\nBackend dispatcher for CPPLS score plots. Use backend=:plotly (default) for the PlotlyJS extension or backend=:makie for the Makie extension.\n\nThe dispatcher accepts backend-agnostic keywords and passes any remaining keywords to the selected backend. To avoid confusion, think of the keywords as belonging to three groups:\n\nGeneral (backend-agnostic)\n\nbackend::Symbol = :plotly Select the backend. Supported values: :plotly, :makie.\n\nPlotlyJS backend keywords (PlotlyJSExtension)\n\ngroup_order::Union{Nothing,AbstractVector} = nothing Order of groups (also draw order; later is on top). If nothing, uses levels(groups) for CategoricalArray, else unique(groups).\ndefault_trace = (;) PlotlyJS scatter kwargs applied to every group (except marker).\ngroup_trace::AbstractDict = Dict() Per-group PlotlyJS scatter kwargs.\ndefault_marker = (;) PlotlyJS marker kwargs for every group (keys must be Symbols).\ngroup_marker::AbstractDict = Dict() Per-group marker kwargs (keys must be Symbols).\nhovertemplate::AbstractString = \"Sample: %{text}<br>Group: %{fullData.name}<br>LV1: %{x}<br>LV2: %{y}<extra></extra>\" Hover text template. The default shows sample, group, LV1, LV2.\nlayout::Union{Nothing,PlotlyJS.Layout} = nothing Layout object; if nothing, a default layout is created using title, xlabel, and ylabel.\nplot_kwargs = (;) Extra kwargs passed to PlotlyJS.plot (e.g., config).\nshow_legend::Union{Nothing,Bool} = nothing If false, sets showlegend=false for all traces.\ntitle::AbstractString = \"Scores\"\nxlabel::AbstractString = \"Latent Variable 1\"\nylabel::AbstractString = \"Latent Variable 2\"\n\nMakie backend keywords (MakieExtension)\n\ngroup_order::Union{Nothing,AbstractVector} = nothing Order of groups (also draw order).\ndefault_scatter = (;) Makie scatter kwargs applied to every group.\ngroup_scatter::AbstractDict = Dict() Per-group scatter kwargs.\ndefault_trace = (;) Additional scatter kwargs applied to every group (legacy convenience).\ngroup_trace::AbstractDict = Dict() Per-group scatter kwargs (legacy convenience).\ndefault_marker = (;) Marker-related kwargs applied to every group.\ngroup_marker::AbstractDict = Dict() Per-group marker kwargs.\ntitle::AbstractString = \"Scores\"\nxlabel::AbstractString = \"Latent Variable 1\"\nylabel::AbstractString = \"Latent Variable 2\"\nfigure = nothing Provide an existing Figure to draw into.\naxis = nothing Provide an existing Axis to draw into.\nfigure_kwargs = (;) Extra kwargs passed to Figure when it is created.\naxis_kwargs = (;) Extra kwargs passed to Axis when it is created.\nshow_legend::Bool = true If true, calls axislegend.\nlegend_kwargs = (;) Extra kwargs passed to axislegend.\nshow_inspector::Bool = true If true, enables DataInspector on GLMakie/WGLMakie.\ninspector_kwargs = (;) Extra kwargs passed to DataInspector.\n\nNotes\n\nThe dispatcher checks that the requested backend is loaded and errors with \"Backend <pkg> not loaded\" if not.\nUnknown backend values throw error(\"Unknown backend\").\nscores must have at least two columns (LV1 and LV2).\n\n\n\n\n\n","category":"function"},{"location":"Utils/statistics/#Statistics","page":"Statistics","title":"Statistics","text":"","category":"section"},{"location":"Utils/statistics/#CPPLS.fisherztrack","page":"Statistics","title":"CPPLS.fisherztrack","text":"fisherztrack(X::AbstractArray{<:Real, 3}, scores::AbstractVector; weights=:mean)\n\nInterpret X as a three-dimensional array of shape n × axis1 × axis2, where n matches the length of scores. For each combination of axis1 and axis2, the corresponding length-n slice is correlated with scores. These correlations are Fisher z-transformed, averaged within each axis1, and inverse-transformed again. When weights = :mean, the slice means are used as weights; when weights = :none, all slices contribute equally. The result is a vector of length size(X, 2) containing one smoothed correlation per axis1.\n\nExamples\n\njulia> X = reshape(Float64[1, 2, 3, 4, 2, 3, 4, 5, 3, 5, 7, 9], 4, 3, 1);\n\njulia> scores = [1.0, 2.0, 3.0, 4.0];\n\njulia> fisherztrack(X, scores) ≈ [1.0, 1.0, 1.0]\ntrue\n\n\n\n\n\n","category":"function"},{"location":"Utils/statistics/#CPPLS.invfreqweights","page":"Statistics","title":"CPPLS.invfreqweights","text":"invfreqweights(samples::AbstractVector)\n\nReturn normalized inverse-frequency weights for samples. Each observation receives weight 1 / count(samples[i]), and the resulting weights are rescaled to sum to 1.\n\n\n\n\n\n","category":"function"},{"location":"Utils/statistics/#CPPLS.intervalize","page":"Statistics","title":"CPPLS.intervalize","text":"intervalize(values::AbstractVector{<:Real})\n\nConvert a sequence of numeric values into adjacent (lo, hi) intervals by pairing consecutive entries. This is useful for building gamma intervals from a range. If values has length 1, a single (x, x) interval is returned. An empty collection throws an ArgumentError.\n\n\n\n\n\n","category":"function"},{"location":"#CPPLS.jl","page":"Home","title":"CPPLS.jl","text":"CPPLS provides a pure-Julia implementation of Canonical Powered Partial Least Squares (CPPLS) for both regression and discriminant analysis. The package is intended for high-dimensional and collinear predictor settings in which latent-variable models are used for supervised dimension reduction, interpretation, and prediction.","category":"section"},{"location":"#Installation","page":"Home","title":"Installation","text":"The package is not registered, so install it directly from GitHub:\n\njulia> ]\npkg> add https://github.com/oniehuis/CPPLS.jl\n\nAfter the installation finishes you can load it in the Julia REPL with:\n\njulia> using CPPLS","category":"section"},{"location":"#Scope","page":"Home","title":"Scope","text":"The package supports supervised latent-variable modelling with either matrix-valued responses or class labels. Fitting is controlled through CPPLSSpec, which separates model configuration from the data passed to fit. The resulting fitted models provide prediction, class assignment, latent projections, regression coefficients, fitted values, residuals, and labels or metadata retained from model fitting.\n\nCPPLS also includes helper functionality for common preprocessing and encoding tasks, including one-hot encoding of class labels and utility functions used in chemometric and multivariate workflows.","category":"section"},{"location":"#Validation-and-Inference","page":"Home","title":"Validation and Inference","text":"The cross-validation interface is designed around explicit evaluation callbacks, so the same machinery can be used for either discriminant analysis or regression. Standard callback bundles are provided by cv_classification() and cv_regression(), and can be supplied directly to nested_cv and nested_cv_permutation. For classification workflows, cv_outlier_scan performs repeated outer-fold evaluation to quantify how often individual samples are misclassified.\n\nPermutation-based significance assessment is available through nested_cv_permutation together with calculate_p_value, allowing empirical evaluation of whether observed predictive performance exceeds what would be expected under response permutation.","category":"section"},{"location":"#Usage","page":"Home","title":"Usage","text":"For fitting and model specification, see CPPLS/fit.md. Prediction, projection, and class-assignment methods are described in CPPLS/predict.md. Cross-validation, permutation testing, and outlier scanning are documented in CPPLS/crossvalidation.md. The main data structures and fitted-model accessors are summarized in CPPLS/types.md, while utility functionality is described under the Utils section of the manual.","category":"section"},{"location":"#Disclaimer","page":"Home","title":"Disclaimer","text":"CPPLS is provided \"as is,\" without warranty of any kind. Users are responsible for independently validating all outputs and interpretations and for determining suitability for their specific applications. The authors and contributors disclaim any liability for errors, omissions, or any consequences arising from use of the software, including use in regulated, clinical, or safety-critical contexts.","category":"section"},{"location":"#References","page":"Home","title":"References","text":"Indahl UG, Liland KH, Naes T (2009) Canonical partial least squares — a unified PLS  approach to classification and regression problems. Journal of Chemometrics 23: 495-504.  https://doi.org/10.1002/cem.1243.\nLiland KH, Indahl UG (2009): Powered partial least squares discriminant analysis.  Journal of Chemometrics 23: 7-18. https://doi.org/10.1002/cem.1186.\nSmit S, van Breemen MJ, Hoefsloot HCJ, Smilde AK, Aerts JMFG, de Koster CG (2007):  Assessing the statistical validity of proteomics based biomarkers. Analytica Chimica  Acta 592: 210-217. https://doi.org/10.1016/j.aca.2007.04.043.","category":"section"},{"location":"CPPLS/fit/#Fit","page":"Fit","title":"Fit","text":"Model fitting in CPPLS is performed through StatsAPI.fit together with a CPPLSSpec. The same interface is used for both regression and discriminant analysis. In both settings, the fit can optionally incorporate obs_weights and Y_aux. Observation weights control how strongly individual samples contribute to the model, whereas auxiliary response information can shape the supervised projection without  changing the primary prediction target. In discriminant analysis (DA), observation weights  are especially useful when classes are imbalanced. Together with gamma, these arguments  are among the main levers for tailoring a CPPLS model to the structure of a particular  dataset.\n\nThe following example illustrates the effect of observation weights and auxiliary responses in a discriminant-analysis workflow.","category":"section"},{"location":"CPPLS/fit/#Example:-DA-with-Observation-Weights-and-Auxiliary-Responses","page":"Fit","title":"Example: DA with Observation Weights and Auxiliary Responses","text":"In this example, we use a synthetic dataset that ships with the package. The dataset contains 100 samples: 10 belong to the minority class minor and 90 to the majority class major. Accordingly, the predictor matrix has 100 rows and 14 columns, representing 14 measured traits. The classes differ across these traits, but the data also include structured variation captured by auxiliary responses. Because that auxiliary  signal is partly correlated with class membership, it can influence the orientation of the latent axes even though it is not itself the primary classification target.\n\nThe dataset was constructed so that a plain PCA score plot is dominated by nuisance structure, whereas CPPLS-DA recovers the class contrast more clearly. Because the dataset combines class imbalance with auxiliary structure that is partly aligned with class membership, it is well suited for illustrating how observation weighting and auxiliary responses alter the fitted score space. Observation weights reduce the influence of the majority class, and auxiliary responses help reveal how auxiliary structure is represented in the latent space.\n\nIn addition to CPPLS, the example uses JLD2 to load the dataset from disk, MultivariateStats to compute the PCA baseline, Colors to convert the auxiliary variable into grayscale values, Statistics to orient the latent variables consistently across plots, and CairoMakie to render static figures.\n\nusing CPPLS\nusing JLD2\nusing MultivariateStats\nusing Statistics\nusing CairoMakie\nusing Colors\n\nsample_labels, X, classes, Y_aux = load(\n    CPPLS.dataset(\"synthetic_cppls_da_dataset.jld2\"),\n    \"sample_labels\",\n    \"X\",\n    \"classes\",\n    \"Y_aux\"\n)\n\nbackend = :makie  # use Makie to render all score plots\nfigure_kwargs = (; size=(900, 600))  # score plot dimensions\n\nfunction orient_scores(scores, classes; reference_class=\"major\")\n    oriented = copy(scores)\n    reference_idx = classes .== reference_class\n    for lv in axes(oriented, 2)\n        if mean(oriented[reference_idx, lv]) < 0\n            oriented[:, lv] .*= -1\n        end\n    end\n    oriented\nend\nnothing # hide\n\nWe start with a Principal Component Analysis (PCA) to inspect the separation of the two classes based on variance alone.\n\nM = fit(PCA, X'; maxoutdim=2)\nscores_pca = permutedims(predict(M, X'))\n\npca_plt = scoreplot(\n    sample_labels,\n    classes,\n    scores_pca;\n    backend=backend,\n    figure_kwargs=figure_kwargs,\n    title=\"PCA scores\",\n    xlabel=\"Principal Component 1\", \n    ylabel=\"Principal Component 2\",\n    default_marker=(; markersize=14)\n)\nsave(\"pca.svg\", pca_plt)\nnothing # hide\n\n(Image: )\n\nThe PCA score plot provides a baseline. In this synthetic dataset, the first two principal  components are not optimized for class discrimination, so a substantial part of the visible  structure reflects nuisance variation rather than the class contrast of interest.\n\nWe next fit a CPPLS-DA model without observation weights or auxiliary responses. We use a fixed gamma=0.5 throughout this example rather than estimating gamma, so that the main differences between the fits come from the inclusion or omission of observation  weights and auxiliary responses. For visual comparability across plots, we orient each  latent variable so that the mean score of class major is positive; this only fixes the  sign indeterminacy of the latent variables and does not change the fit itself.\n\nspec = CPPLSSpec(\n    n_components=2,\n    gamma=0.5,\n    analysis_mode=:discriminant\n)\n\nm_plain = fit(\n    spec,\n    X,\n    classes;\n    sample_labels=sample_labels\n)\n\nscores_plain = orient_scores(X_scores(m_plain)[:, 1:2], classes)\n\ncppls_plain_plt = scoreplot(\n    sample_labels,\n    classes,\n    scores_plain;\n    backend=backend,\n    figure_kwargs=figure_kwargs,\n    title=\"CPPLS-DA scores from an unweighted model without auxiliary responses\",\n    default_marker=(; markersize=14)\n)\nsave(\"cppls_plain.svg\", cppls_plain_plt)\nnothing # hide\n\n(Image: )\n\nFitting CPPLS-DA directly to the class labels yields a more class-oriented score space. Because the classes are imbalanced, however, the majority class exerts more influence on the latent variables than the minority class.\n\nWe now add inverse-frequency observation weights. This gives the two classes the same total influence on the fitted model and reduces the bias introduced by unequal class sizes.\n\nm_weighted = fit(\n    spec,\n    X,\n    classes;\n    obs_weights=invfreqweights(classes),  # give both classes equal total weight\n    sample_labels=sample_labels\n)\n\nscores_weighted = orient_scores(X_scores(m_weighted)[:, 1:2], classes)\n\ncppls_weighted_plt = scoreplot(\n    sample_labels,\n    classes,\n    scores_weighted;\n    backend=backend,\n    figure_kwargs=figure_kwargs,\n    title=\"CPPLS-DA scores from an inverse-frequency-weighted model\",\n    default_marker=(; markersize=14)\n)\nsave(\"cppls_weighted.svg\", cppls_weighted_plt)\nnothing # hide\n\n(Image: )\n\nApplying inverse-frequency weights makes the discriminant structure more symmetric. In this example, the main effect is not a larger distance between the groups, but rather a recentering and slight rotation of the latent space so that the class contrast is less biased by class prevalence.\n\nAt this point, the class separation already looks convincing. In this dataset, however, it is driven not only by class-related variation but also by structured variation associated with auxiliary responses. To address that, we include Y_aux in addition to the observation weights. This changes how the latent variables are estimated. Instead of forcing all supervised structure into the class contrast, the model can also represent variation associated with auxiliary responses.\n\nm_weighted_yaux = fit(\n    spec,\n    X,\n    classes;\n    obs_weights=invfreqweights(classes),  # give both classes equal total weight\n    Y_aux=Y_aux,                          # model auxiliary responses explicitly\n    sample_labels=sample_labels\n)\n\nscores_weighted_yaux = orient_scores(X_scores(m_weighted_yaux)[:, 1:2], classes)\n\ncppls_weighted_yaux_plt = scoreplot(\n    sample_labels,\n    classes,\n    scores_weighted_yaux;\n    backend=backend,\n    figure_kwargs=figure_kwargs,\n    title=\"CPPLS-DA scores from an inverse-frequency-weighted model with auxiliary responses\",\n    default_marker=(; markersize=14)\n)\nsave(\"cppls_weighted_yaux.svg\", cppls_weighted_yaux_plt)\nnothing # hide\n\n(Image: )\n\nThe visible class separation may not have increased much, but it is now more likely to reflect information that is genuinely related to class membership rather than variation carried by a correlated covariate. To make that difference easier to see, we plot the last two score sets again, now shading each point by its auxiliary response value.\n\naux = vec(Y_aux[:, 1])\n\naux_min, aux_max = extrema(aux)\npoint_colors = Gray.(0.1 .+ 0.8 .* ((aux .- aux_min) ./ (aux_max - aux_min)))\n\ncppls_weighted_shaded_plt = scoreplot(\n    sample_labels,\n    fill(\"samples\", length(sample_labels)),\n    scores_weighted;\n    backend=backend,\n    figure_kwargs=figure_kwargs,\n    title=\"CPPLS-DA scores from an inverse-frequency-weighted model \" *\n          \"shaded by auxiliary values\",\n    show_legend=false,\n    default_scatter=(; color=point_colors),\n    default_marker=(; markersize=14)\n)\nsave(\"cppls_weighted_shaded.svg\", cppls_weighted_shaded_plt)\n\ncppls_weighted_yaux_shaded_plt = scoreplot(\n    sample_labels,\n    fill(\"samples\", length(sample_labels)),\n    scores_weighted_yaux;\n    backend=backend,\n    figure_kwargs=figure_kwargs,\n    title=\"CPPLS-DA scores from an inverse-frequency-weighted model with auxiliary responses \" *\n          \"shaded by auxiliary values\",\n    show_legend=false,\n    default_scatter=(; color=point_colors),\n    default_marker=(; markersize=14)\n)\nsave(\"cppls_weighted_yaux_shaded.svg\", cppls_weighted_yaux_shaded_plt)\nnothing # hide\n\n(Image: )\n\n(Image: )\n\nIn the first shaded score plot, fitted without auxiliary response information, the grayscale values are not randomly distributed across the score space. Instead, they are arranged roughly along the first latent dimension. This indicates that auxiliary signal correlated with class membership has leaked into the apparent class separation.\n\nWith the auxiliary response information included, as shown in the second plot, the auxiliary structure is much less pronounced in the fitted scores, as indicated by the grayscale values being more evenly distributed across the plot.\n\nOverall, the example shows how observation weights can rebalance class influence and how auxiliary responses can help separate auxiliary structure from the signal that is genuinely relevant to group membership.","category":"section"},{"location":"CPPLS/fit/#API","page":"Fit","title":"API","text":"The example above illustrates a discriminant-analysis workflow, but the same fitting entry point is used more generally throughout CPPLS. The reference below documents the fit interface itself.","category":"section"},{"location":"CPPLS/fit/#StatsAPI.fit","page":"Fit","title":"StatsAPI.fit","text":"fit(model::CPPLSSpec,\n    X::AbstractMatrix{<:Real},\n    Y_prim::AbstractMatrix{<:Real};\n    kwargs...\n)\nfit(model::CPPLSSpec,\n    X::AbstractMatrix{<:Real},\n    sample_classes::AbstractCategoricalArray{T,1,R,V,C,U};\n    kwargs...\n) where {T,R,V,C,U}\nfit(model::CPPLSSpec,\n    X::AbstractMatrix{<:Real},\n    sample_classes::AbstractVector;\n    kwargs...\n)\n\nFit a CPPLS model using the StatsAPI entry point and an explicit CPPLSSpec. The model specification supplies the number of components, the gamma configuration, centering, the analysis mode, and all numerical tolerances, while the call to fit supplies data, optional weights, auxiliary responses, and label metadata.\n\nWhen Y_prim is provided, it is treated as the primary response block. When sample_classes is provided, the labels are converted to a one-hot response matrix, class names are inferred as response labels, and the fit is forced to discriminant analysis; model.analysis_mode must be :discriminant or an ArgumentError is thrown.\n\nThe gamma setting in model may be a fixed scalar, a (lo, hi) tuple, or a vector mixing scalars and tuples. Non-scalar settings trigger per-component selection by choosing the value that yields the largest leading canonical correlation between the supervised projection and the primary responses, and the resulting gamma values are stored in the fitted model.\n\nKeyword arguments accepted by fit include obs_weights for per-sample weighting, Y_aux for auxiliary response columns, and optional sample_labels, predictor_labels, response_labels, and sample_classes metadata for diagnostics and plotting. Y_aux must have the same number of rows as X and is concatenated to Y_prim internally to build the supervised projection, while prediction targets always remain the primary responses.\n\nThe return value is a CPPLSFit containing scores, loadings, regression coefficients, and the metadata needed for downstream prediction and diagnostics. Use CPPLS.fit or StatsAPI.fit when disambiguation is required in your namespace.\n\nSee also CPPLSFit,  CPPLSSpec,  coef,  fitted,  gamma,  invfreqweights, predictor_labels, response_labels, residuals, sample_classes, sample_labels, X_scores\n\nExamples\n\njulia> using JLD2; file = CPPLS.dataset(\"synthetic_cppls_da_dataset.jld2\");\n\njulia> labels, X, classes, Y_aux = load(file, \"sample_labels\", \"X\", \"classes\", \"Y_aux\");\n\njulia> spec = CPPLSSpec(n_components=2, gamma=0.01:0.01:1.00, analysis_mode=:discriminant)\nCPPLSSpec\n  n_components: 2\n  gamma: 0.01:0.01:1.0\n  center: true\n  analysis_mode: discriminant\n\njulia> model = fit(spec, X, classes; sample_labels=labels);\n\njulia> size(CPPLS.X_scores(model))\n(100, 2)\n\njulia> spec = CPPLSSpec(n_components=2, gamma=0.75, analysis_mode=:discriminant)\nCPPLSSpec\n  n_components: 2\n  gamma: 0.75\n  center: true\n  analysis_mode: discriminant\n\njulia> model = fit(spec, X, classes; obs_weights=invfreqweights(classes), Y_aux=Y_aux)\nCPPLSFit\n  mode: discriminant\n  samples: 100\n  predictors: 14\n  responses: 2\n  components: 2\n  gamma: [0.75, 0.75]\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/predict/#Projection-and-Prediction","page":"Projection and Prediction","title":"Projection and Prediction","text":"After a CPPLS model has been fitted with fit, it can be applied in two main ways. project maps new samples into the latent score space defined by the model, whereas predict generates predicted responses from new predictor values.\n\nFor discriminant models, CPPLS also provides helpers for turning raw prediction arrays into class assignments. predictions_to_onehot and predictions_to_sampleclasses perform those conversions when you already have the output of predict. For convenience, predictonehot returns  one-hot encoded class predictions directly, and predictsampleclasses returns  predicted class labels.","category":"section"},{"location":"CPPLS/predict/#Example","page":"Projection and Prediction","title":"Example","text":"The example below reuses the synthetic discriminant-analysis dataset introduced on the Fit page. We hold out one sample from each class, fit a CPPLS-DA model on the remaining observations, and then use the fitted model to:\n\nproject the held-out samples into the latent space, and\npredict their class membership.\n\nWe start by loading the synthetic data and splitting them into a training set and a hold-out set.\n\nusing CPPLS\nusing JLD2\n\nsample_labels, X, classes, Y_aux = load(\n    CPPLS.dataset(\"synthetic_cppls_da_dataset.jld2\"),\n    \"sample_labels\",\n    \"X\",\n    \"classes\",\n    \"Y_aux\"\n)\n\nholdout_idx = [findlast(==(\"minor\"), classes), findlast(==(\"major\"), classes)]\ntrain_idx = setdiff(collect(axes(X, 1)), holdout_idx)\n\nX_train = X[train_idx, :]\nclasses_train = classes[train_idx]\nY_aux_train = Y_aux[train_idx, :]\nlabels_train = sample_labels[train_idx]\n\nX_holdout = X[holdout_idx, :]\nclasses_holdout = classes[holdout_idx]\nlabels_holdout = sample_labels[holdout_idx]\nplot_classes_holdout = [\"projected $class\" for class in classes_holdout]\nnothing # hide\n\nWe next fit a discriminant model with two latent components and allow gamma to be  selected during fitting.\n\nspec = CPPLSSpec(\n    n_components=2,\n    gamma=0:0.1:1,\n    analysis_mode=:discriminant\n)\n\nmodel = fit(\n    spec,\n    X_train,\n    classes_train;\n    obs_weights=invfreqweights(classes_train),\n    Y_aux=Y_aux_train,\n    sample_labels=labels_train\n)\nnothing # hide\n\nWe can now apply the fitted model to the held-out samples. We first use project  to obtain latent scores and then plot the held-out samples together with the training  samples in the fitted score space. The returned heldout_scores matrix has one row per  held-out sample and one column per latent component.\n\nheldout_scores = project(model, X_holdout)\n\nprojected_plt = scoreplot(\n    vcat(labels_train, labels_holdout),\n    vcat(classes_train, plot_classes_holdout),\n    vcat(X_scores(model), heldout_scores);\n    backend=:makie,\n    figure_kwargs=(; size=(900, 600)),\n    title=\"CPPLS-DA scores\",\n    default_marker=(; markersize=14)\n)\nsave(\"projected.svg\", projected_plt)\nnothing # hide\n\n(Image: )\n\nThe two projected samples fall near the clusters of the classes from which they were held out. That visual impression suggests that the model should classify them as minor and major, respectively, but prediction lets us check that conclusion more directly.\n\nThe true class labels of the held-out samples are:\n\nclasses_holdout\n\nLet us now see what the model predicts:\n\nheldout_predictions = predict(model, X_holdout)\npredictions_to_sampleclasses(model, heldout_predictions)\n\nAs we can see, the predicted labels match the classes from which the samples were drawn. In this example, heldout_predictions is a three-dimensional array whose third dimension indexes the number of components used in the prediction.\n\nInstead of calling predict and predictions_to_sampleclasses successively, we could have used the convenience wrapper predictsampleclasses. More generally, predictonehot and predictsampleclasses collapse the full prediction tensor into class assignments, which is often more convenient in discriminant-analysis workflows.\n\npredictsampleclasses(model, X_holdout)","category":"section"},{"location":"CPPLS/predict/#API","page":"Projection and Prediction","title":"API","text":"","category":"section"},{"location":"CPPLS/predict/#StatsAPI.predict","page":"Projection and Prediction","title":"StatsAPI.predict","text":"predict(model::AbstractCPPLSFit, X::AbstractMatrix{<:Real},\n    n_components::Integer=size(model.B, 3)) -> Array{Float64, 3}\n\nGenerate predictions from a fitted CPPLS model for the rows of X. The result is a (n_samples, n_targets, n_components) array where [:, :, i] contains predictions  using the first i components. A DimensionMismatch is thrown if n_components exceeds the number stored in the model.\n\nExamples\n\njulia> coeffs = reshape(Float64[0.5, 1.0], 2, 1, 1);  # two predictors, one target\n\njulia> X_bar = zeros(1, 2); Y_bar = reshape([0.0], 1, 1);\n\njulia> model = CPPLSFitLight(coeffs, X_bar, Y_bar, :regression);\n\njulia> Xnew = [1.0 2.0; 3.0 4.0];\n\njulia> predict(model, Xnew) ≈ [2.5; 5.5]\ntrue\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/predict/#CPPLS.predictonehot","page":"Projection and Prediction","title":"CPPLS.predictonehot","text":"predictonehot(\n    model::AbstractCPPLSFit,\n    X::AbstractMatrix{<:Real},\n    n_components::Integer=size(model.B, 3),\n) -> Matrix{Int}\n\nGenerate one-hot encoded class predictions from a fitted CPPLS model and predictors X. This calls predict, sums predictions across components, corrects for repeated mean addition, and assigns each sample to the highest-scoring class.\n\nExamples\n\njulia> coeffs = reshape(Float64[1, -1, 0.5, -0.5], 2, 2, 1);  # two predictors, two classes\n\njulia> X_bar = zeros(1, 2); Y_bar = reshape([0.0 0.0], 1, 2);\n\njulia> model = CPPLSFitLight(coeffs, X_bar, Y_bar, :regression);\n\njulia> Xnew = [2.0 1.0; 0.5 3.0];\n\njulia> predictonehot(model, Xnew) ≈ [1 0; 0 1]\ntrue\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/predict/#CPPLS.predictsampleclasses","page":"Projection and Prediction","title":"CPPLS.predictsampleclasses","text":"predictsampleclasses(\n    model::CPPLSFit,\n    X::AbstractMatrix{<:Real},\n    n_components::Integer=size(model.B, 3),\n) -> AbstractVector\n\nGenerate predicted class labels from a discriminant CPPLS model and predictors X. The returned vector follows the ordering in response_labels.\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/predict/#CPPLS.predictions_to_onehot","page":"Projection and Prediction","title":"CPPLS.predictions_to_onehot","text":"predictions_to_onehot(\n    model::AbstractCPPLSFit,\n    predictions::AbstractArray{<:Real, 3},\n) -> Matrix{Int}\n\nConvert a 3D prediction tensor (as returned by predict) into a one-hot encoded matrix. Predictions are summed across components and corrected for repeated mean addition before selecting the highest-scoring class for each sample.\n\nExamples\n\njulia> coeffs = reshape(Float64[1, -1, 0.5, -0.5], 2, 2, 1);  # two predictors, two classes\n\njulia> X_bar = zeros(1, 2); Y_bar = reshape([0.0 0.0], 1, 2);\n\njulia> model = CPPLSFitLight(coeffs, X_bar, Y_bar, :regression);\n\njulia> Xnew = [2.0 1.0; 0.5 3.0];\n\njulia> raw = predict(model, Xnew);\n\njulia> predictions_to_onehot(model, raw) ≈ [1 0; 0 1]\ntrue\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/predict/#CPPLS.predictions_to_sampleclasses","page":"Projection and Prediction","title":"CPPLS.predictions_to_sampleclasses","text":"predictions_to_sampleclasses(\n    model::CPPLSFit,\n    predictions::AbstractArray{<:Real, 3},\n) -> AbstractVector\n\nConvert a 3D prediction tensor (as returned by predict) into class labels using the stored response_labels ordering.\n\n\n\n\n\n","category":"function"},{"location":"CPPLS/predict/#CPPLS.project","page":"Projection and Prediction","title":"CPPLS.project","text":"project(model::AbstractCPPLSFit, X::AbstractMatrix{<:Real}) -> AbstractMatrix\n\nCompute latent component scores by projecting new predictors X with a fitted CPPLS model. The predictors are centered using model.X_bar and multiplied by model.R, returning an (n_samples, n_components) score matrix.\n\nExamples\n\njulia> struct DemoCPPLS <: CPPLS.AbstractCPPLSFit\n           R::Matrix{Float64}\n           X_bar::Matrix{Float64}\n       end\n\njulia> proj = reshape([1.0, 0.5], 2, 1)\n2×1 Matrix{Float64}:\n 1.0\n 0.5\n\njulia> demo = DemoCPPLS(proj, reshape([0.5, 0.5], 1, :));\n\njulia> project(demo, [1.0 2.0; 3.0 4.0]) ≈ [1.25; 4.25]\ntrue\n\nIn practice, demo would be the CPPLSFit object returned by fit_cppls, which already contains the appropriate R matrix and predictor means.\n\n\n\n\n\n","category":"function"}]
}
